extends layout

block content
  h1= title
  p Welcome to #{title}
  form#songSearch(action='/search', method='POST')
    | Name of song:
    input(type='text', name='query')
    input(type='submit', value='Search for song')