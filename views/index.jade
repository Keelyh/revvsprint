extends layout

block content
  h1= title
  div(class = 'songs')
    form
      input(type='text', name = 'song', placeholder = 'Song Name')
      input(type='text', name = 'activity', placeholder = 'Activity')
      input(type='text', name = 'time', placeholder = 'Time')
      input(type='submit', value = 'Search Song', class = 'songsearch', onClick = "showsongs()")
      input(type='submit', value = '-',onClick="$(this).parent().remove()", class = 'removefield')
  input(type='submit', value = '+', class = 'addsong')

  script
    $(function () {
      function showsongs (){
        console.log("hello bitch");
        // $.post('/showsongs', $(this).parent().serialize(), function(html) {
        //   $(this).parent().append(html);
        // });
        return false;
      }
      return false;
    });
  script
    $('.addsong').click(function() {
      $('<form><input type="text" name="song" placeholder="Song Name"/><input type="text" name="activity" placeholder="Activity"/><input type="text" name="time" placeholder="Time"/><input type="submit" value="Search Song" class="removefield"/><input type="submit" value="-" class="removefield" onClick="$(this).parent().remove();"/></form>').appendTo('.songs');
    return false;
    });

    var remove_p = function() {
      $(this).parent().remove();
      console.log('remove');
      return false;
    };
